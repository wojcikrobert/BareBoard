/*#include "DataTypes.h"
#include "BoardSetup.h"
#include "setjmp.h"
#include "ContextManager.h"

jmp_buf  env;
*/

int main(void) {

   /*if(TRUE == InitBoard()){

      SaveContext();
      if(setjmp(env)){
         SetOutput(LD1,TRUE);
      }
      else{
         longjmp(env,1);
      }
      while(1){
      }
   }
   else{

   }*/
   return -1;
}
